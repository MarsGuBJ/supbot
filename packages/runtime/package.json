{
  "name": "@supbot/runtime",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build -w @supbot/shared && vitest run"
  },
  "dependencies": {
    "@supbot/shared": "file:../shared",
    "unzipper": "^0.12.3"
  },
  "devDependencies": {
    "vitest": "^4.1.9"
  }
}
